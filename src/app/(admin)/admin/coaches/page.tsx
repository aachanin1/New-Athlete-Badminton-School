import { PagePlaceholder } from '@/components/shared/page-placeholder'

export default function CoachesPage() {
  return <PagePlaceholder title="จัดการโค้ช" description="เพิ่ม แก้ไข ข้อมูลโค้ช และกำหนดหัวหน้าโค้ช" />
}
