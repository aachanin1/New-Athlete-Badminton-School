import { PagePlaceholder } from '@/components/shared/page-placeholder'

export default function PayrollPage() {
  return <PagePlaceholder title="เงินเดือนโค้ช" description="คำนวณชั่วโมงสอนและค่าตอบแทนโค้ช (OT เกิน 25 ชม./สัปดาห์)" />
}
