import { PagePlaceholder } from '@/components/shared/page-placeholder'

export default function AttendancePage() {
  return <PagePlaceholder title="เช็คชื่อนักเรียน" description="เช็คชื่อนักเรียนในแต่ละรอบเรียน" />
}
